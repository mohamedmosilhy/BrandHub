# Presentation — everything the user sees

**May contain:** Screens, components, view-model hooks, theme and formatting. Depends on `domain` and `core`.

**May not contain:** Imports from `data` or `infrastructure`. A component that fetches its own data.
