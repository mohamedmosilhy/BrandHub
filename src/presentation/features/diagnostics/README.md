# Diagnostics feature

**May contain:** Development-only screens that show the app's own state, starting with the
environment screen that proves the configuration pipeline end to end.

**May not contain:** Anything shipped to users. This feature is removed or hidden behind the
dev-menu flag before release.
