# Diagnostics view-models

**May contain:** Query orchestration over domain repository ports for development diagnostics.

**May not contain:** DTOs, endpoint paths, HTTP clients or repository implementations.
