# Presentation — navigation shell

**May contain:** Phase-shell screens and small display-only sections used by the typed navigators.

**May not contain:** Navigation state, route parsing, repositories, HTTP, or storage.
