# Data — identity

**May contain:** Validated auth DTOs, pure user/session mappers, remote and secure local data
sources, and the HTTP authentication repository.

**May not contain:** DTO leakage into domain or presentation, or tokens in AsyncStorage.
