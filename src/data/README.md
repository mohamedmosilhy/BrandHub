# Data — implements the domain's ports

**May contain:** Repository implementations, data sources, DTOs and mappers. Depends on `domain`, `core` and `infrastructure`.

**May not contain:** Imports from `presentation` or `app`. Returning a DTO to a caller, or throwing a transport error.
