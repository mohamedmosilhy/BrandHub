# Infrastructure — external technical concerns

**May contain:** HTTP client, storage, config, i18n and logging, each behind a small port.

**May not contain:** Imports from `domain`, `data`, `presentation` or `app`. Business rules of any kind.
