# HTTP

**May contain:** The `HttpClient` port, Axios adapter, correlation ids, error normalization and
single-flight refresh interceptors.

**May not contain:** Endpoint URLs — those belong to data sources.
