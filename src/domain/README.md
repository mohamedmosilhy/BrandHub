# Domain — the centre

**May contain:** Entities, value objects, use cases, repository ports and business rules. Plain TypeScript, runnable in Node.

**May not contain:** Imports of React, React Native, HTTP clients, storage, navigation, DTOs, or anything from `data`, `infrastructure`, `presentation` or `app`.
