# Dependency container

**May contain:** The single construction file that binds ports to implementations and the typed
provider/accessor that exposes those bindings.

**May not contain:** Anything else. If it is not wiring, it does not belong here.
