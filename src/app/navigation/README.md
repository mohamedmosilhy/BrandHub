# App — navigation

**May contain:** Typed root, modal, tab and per-tab stack composition, linking configuration, and
authentication guards.

**May not contain:** Feature business logic, DTOs, repository construction, or screen UI.

Phase 6 replaces the Home, Browse, Category and Search shell registrations while retaining the
Phase 5 route names, params, nested-stack ownership and public browsing policy.
