# Source root

**May contain:** The six layers plus the test kit. Layer edges are enforced by `eslint-plugin-boundaries`; see `architecture.md` §9.

**May not contain:** Nothing lives directly in `src/`. Every file belongs to a layer.
