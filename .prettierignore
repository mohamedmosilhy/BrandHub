node_modules
design-reference
coverage
.expo
dist
package-lock.json

# API contract exports are reference data, not source
docs/*.json

# Generated by `npm run mock:reset` and rewritten by the mock server at runtime, so it is
# never hand-edited and formatting it would make `verify` fail after any dev session.
mock-server/db.json
